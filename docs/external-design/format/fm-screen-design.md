
# 画面設計書

## 1. 画面一覧
| 画面ID | 画面名 | 画面種別 | 概要 | 主な利用者 | 遷移元 | 遷移先 |
|---|---|---|---|---|---|---|
| SC-xxx | xxx画面 | 一覧/詳細/登録... | xxx | xxx | xxx | xxx |

## 2. 画面詳細

### 2.x 【SC-xxx】画面名
#### 2.x.1 概要
- 目的:
- 利用者（ロール）:
- 画面種別（一覧/詳細/登録/更新/検索/ダッシュボード等）:
- 前提条件（ログイン/権限/状態など）:
- 入力:
- 出力:
- 関連機能（function-list.mdの機能ID等）:

#### 2.x.2 画面構成
- エリア構成（例: ヘッダ/検索条件/一覧/詳細/フッタ/モーダル）:
- 初期表示時の状態（初期値、初期検索の有無、初期フォーカス）:
- 非同期/ローディング表示方針（必要時）:

#### 2.x.3 画面項目一覧（必須）
※「各項目」について、型/必須/制約/初期値/表示条件/編集可否/一覧列の表示形式を明記

| 項目ID | 項目名 | 種別(表示/入力/選択) | UI部品(テキスト/セレクト/日付/チェック等) | データ型 | 必須 | 制約(桁/形式/範囲) | 初期値 | 表示条件 | 編集可否 | 備考 |
|---|---|---|---|---|---|---|---|---|---|---|
| F-xxx | xxx | 入力 | テキスト | string | Y | 1..50 | - | - | 可 | 用語集に合わせる |

#### 2.x.4 項目別アクション定義（必須：項目→アクション）
※画面項目から発生するイベントごとに、「何がトリガーで、何をして、どうなるか」を具体化すること
※Web特有：API呼出/ローディング/多重送信防止/成功失敗のUIを必ず書くこと

| 項目ID | トリガー（ユーザー操作/イベント） | クライアント側バリデーション | サーバー側バリデーション/業務ルール | 処理内容 | 呼出API/参照先 | ローディング/多重送信防止 | 成功時の結果（UI更新/遷移/通知） | 失敗時（エラー表示/復帰） |
|---|---|---|---|---|---|---|---|---|
| F-xxx | フォーカスアウト | 必須/形式 | 相関チェック | 入力値確定→整形 | API-xxx | スピナー表示 | フィールド補完 | error-rule準拠で表示 |

#### 2.x.5 画面アクション一覧（必須：ボタン/リンク/操作）
※ボタン・リンク・行クリック・ページング・ソート・検索・CSV出力・戻る等を漏れなく列挙
※登録/更新/削除は二重送信防止と確認ダイアログ方針も書く

| アクションID | 操作（UI要素） | トリガー | 入力 | 事前条件 | 処理 | 呼出API | ローディング/多重送信防止 | 遷移先 | 成功時メッセージ/通知 | 失敗時（エラーコード/表示） |
|---|---|---|---|---|---|---|---|---|---|---|
| A-xxx | 登録ボタン | click | 入力項目一式 | バリデーションOK | 登録処理 | API-CreateX | disable+スピナー | SC-yyy | “登録しました” | error-rule準拠 |

#### 2.x.6 一覧/検索の設計（該当する場合のみ）
- 検索条件（項目、デフォルト、部分一致/完全一致等）:
- 検索実行タイミング（検索ボタン/条件変更で自動等）:
- 一覧（列、表示形式、ソート可否、行クリック動作）:
- ページング（件数、ページサイズ変更、総件数表示）:
- 0件時表示（空状態）:

#### 2.x.7 バリデーション／エラー設計（必須）
- 入力チェック一覧（項目名: 条件, メッセージ, 表示位置）
- エラー分類（入力エラー/業務エラー/権限/システム/タイムアウト）と画面上の表示方針（rules/error-rule.md準拠）
- セッションタイムアウト時の扱い（ログイン誘導/再試行不可など、必要があれば）

#### 2.x.8 権限制御（該当する場合のみ）
- ロール別の表示/編集可否:
- ボタン活性/非活性条件:

#### 2.x.9 画面遷移
- 遷移元:
- 遷移先:
- 遷移条件（権限/状態/入力条件など）:
- ブラウザ戻る/リロード時の期待動作（必要があれば）:

#### 2.x.10 前提・要確認事項
- 前提:
- 要確認:
