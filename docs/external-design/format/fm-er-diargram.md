# ER図（ERD）

## 1. エンティティ一覧
- エンティティ名（種別：マスタ/トラン/履歴/ログ）：概要：根拠（どの処理設計のどの記述から抽出したかを簡潔に）

## 2. ER図（Mermaid）
```mermaid
erDiagram
  %% 例:
  %% CUSTOMER ||--o{ ORDER : places
  %% ORDER ||--|{ ORDER_ITEM : contains