# AI駆動開発 手順書

## 目次

1. [本手順書の目的と対象読者](#1-本手順書の目的と対象読者)
    - [1-1. 本手順書の目的](#1-1-本手順書の目的)
    - [1-2. 本手順書の対象者](#1-2-本手順書の対象者)
    - [1-3. ゴール（完成状態）](#1-3-ゴール完成状態)
2. [開発フローの全体概要](#2-開発フローの全体概要)
    - [2-1. 概要説明](#2-1-概要説明)
    - [2-2. 最短実施フロー](#2-2-最短実施フロー)
    - [2-3. 開発フローにおける補足](#2-3-開発フローにおける補足)
3. [事前準備](#3-事前準備)
    - [3-1. 説明概要](#3-1-説明概要)
    - [3-2. フォルダ構成の説明](#3-2-フォルダ構成の説明)
      - [全体像](#全体像)
      - [フォルダ構成の説明](#フォルダ構成の説明)
    - [3-3. プロンプトの実行方法（共通）](#3-3-プロンプトの実行方法共通)
    - [3-4. ローカル環境構築](#3-4-ローカル環境構築)
4. [フェーズ別手順](#4-フェーズ別手順)
    - [4-1. 要件定義](#4-1-要件定義)
    - [4-2. 設計](#4-2-設計)
    - [4-3. フロントエンド実装](#4-3-フロントエンド実装)
    - [4-4. バックエンド実装](#4-4-バックエンド実装)
    - [4-5. コードレビュー](#4-5-コードレビュー)
    - [4-6. 単体テスト](#4-6-単体テスト)
    - [4-7. システムテスト](#4-7-システムテスト)
5. [補足資料](#5-補足資料)
    - [A. プロンプト一覧](#a-プロンプト一覧)
    - [B. 設計フォーマット一覧（`docs/external-design/format/`）](#b-設計フォーマット一覧docsexternal-designformat)
    - [C. ルール・規約ファイル一覧（`docs/rules/`）](#c-ルール規約ファイル一覧docsrules)
    - [D. ローカル開発環境セットアップ（コマンドまとめ）](#d-ローカル開発環境セットアップコマンドまとめ)
    - [E. テストツール及び実行コマンドまとめ](#e-テストツール及び実行コマンドまとめ)
    - [F. 重要注意事項](#f-重要注意事項)
    - [G. AWS環境構築（AWS環境構築担当者向け・簡易）](#g-aws環境構築aws環境構築担当者向け簡易)
    - [H. デプロイ環境構築 CI/CD（CI/CD構築担当者向け・簡易）](#h-デプロイ環境構築-cicdcicd構築担当者向け簡易)

---

## 1. 本手順書の目的と対象読者

### 1-1. 本手順書の目的

本手順書は、**GitHub Copilot（エージェントモード）を中心とした AI 駆動開発**の進め方を説明した資料であり、 Web アプリケーションを「要件定義」から「システムテスト」まで一気通貫で開発・検証するための手順となっている。

### 1-2. 本手順書の対象者

  対象者：**開発者**  
  設計や実装、試験での各成果物の作成を担当する開発者  
  **本手順書は、実開発を行う人が"どう操作してAI駆動開発を行うか"を説明する**  

### 1-3. ゴール（完成状態）

**ゴール**：各工程での成果物をプロンプトのみで出力できる

| ゴール       | 内容                                                                                           |
| :----------- | :--------------------------------------------------------------------------------------------- |
| 設計         | 要件定義書の内容を網羅し、実装工程に必要な粒度の各種設計書が生成されている                     |
| 実装         | 設計書の内容に即したコードが生成され、また、そのコードが要件通りの動作が可能なものになっている |
| レビュー     | 作成したコード/テストコードをレビューし、分析結果となる資料が生成される                        |
| 単体テスト| テストコードを実行し、ガバレッジ計測の結果がプロンプトによって生成されている                   |
| システムテスト | テスト観点、テスト項目が生成、及びテスト実施結果までプロンプトで実行/生成されている                  |

---

## 2. 開発フローの全体概要

### 2-1. 概要説明

この章では、AI駆動開発の実施フローを説明する。
各工程ごとに用意されたプロンプトを実行するだけで、設計～試験までの成果物が生成できるような構成となっている。
直ぐにローカル環境で検証したい場合は、[第４章](#4-フェーズ別手順)に進む。

### 2-2. 最短実施フロー

```text
要件定義（手動）
  └─ 要件定義書・機能一覧を作成する
        ↓
外部設計（実行プロンプト：design-*.prompt.md）
  ├─ アーキテクチャ設計・シーケンス図・処理設計書
  ├─ 画面設計書・ER図・DB設計書
  └─ インタフェース設計書・API一覧
        ↓
実装（実行プロンプト：develop-*.prompt.md）
  ├─ フロントエンド（画面単位）
  └─ バックエンド（一括）
        ↓
レビュー（実行プロンプト：review-*.prompt.md）
  ├─ フロントエンドレビュー
  └─ バックエンドレビュー
        ↓
単体テスト（実行プロンプト：test-unit-*.prompt.md）
  ├─ フロントエンド
  └─ バックエンド
        ↓
システムテスト（A実行プロンプト：test-system-*.prompt.md）
  └─ 観点表 → テストコード → テストデータ → 項目書 → 実施・結果反映
```

### 2-3. 開発フローにおける補足

**実行環境**  
本手順書を作成するにあたり、AWSで環境を構築し検証している。  
そのため、実行環境を用意したい場合には下記の付録を参照する。  
参照：[G. AWS環境構築（AWS環境構築担当者向け・簡易）](#g-aws環境構築aws環境構築担当者向け簡易)

**ライブラリ管理及びデプロイ**  
本手順書を作成するにあたり、ライブラリ管理及びデプロイはGitHubを使用している。  
GitHubでのデプロイを実施したい場合には、下記の付録を参照する。  
参照：[H. デプロイ環境構築 CI/CD（CI/CD構築担当者向け・簡易）](#h-デプロイ環境構築-cicdcicd構築担当者向け簡易)

---

## 3. 事前準備

### 3-1. 説明概要

この章では、[第４章](#4-フェーズ別手順)の開発手順を進める上で前提となる情報を記載している。

### 3-2. フォルダ構成の説明

GithubCopilotでAI駆動開発をする場合のフォルダ構成は下記の通りである。  
**補足**：詳しいファイルの役割は、[補足資料](#5-補足資料)に記載してあるため、適宜参照する。

#### 全体像

```text
project-root/
├── AGENTS.md                     # プロジェクト共通ガイド
│
├── .github/
│   ├── copilot-instructions.md   # Copilot 共通の振る舞い（常時適用）
│   ├── instructions/             # スコープ別 Instructions（ファイルパターンで自動適用）
│   │   ├── frontend.instructions.md
│   │   └── backend.instructions.md
│   └──  prompts/                  # 設計/実装/試験で使用するプロンプトを格納する
│
├── docs/                         # ドキュメント
│   ├── requirement-definition/   # インプットとなる資料（要件定義書、機能一覧等）
│   ├── external-design/
│   │   ├── format/               # 設計書のフォーマット
│   │   └── product/              # 設計書成果物
│   ├── rules/                    # 各種規約/ルール  
│   └── tests/                    # テスト観点・試験項目書
│
├── frontend/                     # フロントエンド実装用フォルダ
│
└── backend/                      # バックエンド実装用フォルダ

```

#### フォルダ構成の説明

| フォルダ                          | 役割                         | AIとの関係         |
| :-------------------------------- | :--------------------------- | :----------------- |
| `docs/requirement-definition/`    | 要件定義**人間が作成**       | AIへの入力         |
| `.github/copilot-instructions.md` | 工程別の指示書（プロンプト） | AIへの入力         |
| `docs/external-design/product/`   | 設計書成果物                 | AIの出力先         |
| `docs/types/`                     | 試験工程成果物               | AIの出力先         |
| ` frontend/ *`  `backend/*`       | 実装対象                     | AIの出力先         |
| `AGENTS.md`                       | プロジェクト共通のルール     | AIが参照するルール |
| `.github/copilot-instructions.md` | Copilot共通の振る舞い        | AIが参照するルール |
| `.github/instructions/`           | スコープ別の常時ルール       | AIが参照するルール |
| `docs/rules/`                     | 各種規約/ルール              | AIが参照するルール |
| `docs/external-design/format/`    | 設計書テンプレート           | AIが参照するルール |

### 3-3. プロンプトの実行方法（共通）

Prompt Files は `.github/prompts/` に配置されている。Copilot Chat（エージェントモード）でファイル名を指定することで、プロンプトの実行が可能である。

1. VS Code で Copilot Chat を開き、**エージェントモード**[^1]を選択する
2. チャット入力欄で対象のプロンプトファイルを指定する（`/` の後ろにファイル名を記載）
   ```
   例： /design-10-architecture
   ```
3. 画像入力が必要なプロンプト（画面デザイン・業務フロー図）は、**PNG をチャットに直接添付**[^2]する
4. 生成結果を確認し、設計書・規約との整合性をレビューする

[^1]:エージェントモードは、GithubCopilotがファイルの追加/編集を行えるようにするモード
[^2]:添付はGithubCopilotのチャット欄にドラッグ&ドロップまたは"+"ボタンから対象ファイルを参照する

### 3-4. ローカル環境構築

本手順書は、Python/Typescriptにて実装/検証をしている。  
実際にローカルで検証してみたい場合には、下記の付録を参照する。  
参照：[付録D](#d-ローカル開発環境セットアップコマンドまとめ)

---

## 4. フェーズ別手順

以下より実際の開発手順となる。  
各工程は「目的／入力／実施手順／出力／確認ポイント」で構成しており、番号順に進める。  
なお、生成AIへの最初のインプット資料となる要件定義工程の成果物は人の手で作成することを前提としている。

### 4-1. 要件定義

##### 目的

後続の工程のインプットとなる要件定義資料を作成する。  
**要件定義資料は次工程で、GitHubCopilotへ添付する。**  

#### 確認ポイント

- マークダウン形式で作成しているか
**注意** 本手順書では要件定義書、機能一覧、業務フロー図を要件定義工程の成果物としている  

---

### 4-2. 設計

#### 目的

要件定義書をもとに、実装の設計図となる外部設計書一式をAIに生成させる。  

#### 入力

- 要件定義書・機能一覧（`docs/requirement-definition/`）
- 画面デザイン・業務フロー図（PNGファイル。該当するプロンプトのみ）
- 前工程で生成した設計書（後続の設計書の入力となる）

#### 実施手順

設計書には依存関係があるため、下表の「実行順」で各プロンプトを実行する。出力先はすべて `docs/external-design/product/` となる。

1. Copilot Chat（エージェントモード）で対象のプロンプトを実行する
2. 画像が必要なもの（シーケンス図・画面設計書）は、PNGをチャットに添付する
3. 生成された設計書を確認し、前工程の設計書を次工程の入力とする

| 実行順 | プロンプト                   | 出力する設計書（要点）                                           | 主な入力                                   |
| :----: | :--------------------------- | :--------------------------------------------------------------- | :----------------------------------------- |
|   1    | `design-10-architecture`     | アーキテクチャ設計書（構成図・コンポーネント役割・データフロー） | 要件定義書                                 |
|   2    | `design-20-sequence-diagram` | シーケンス図（正常系／代表的な異常系。Mermaid）                  | 要件定義書・機能一覧・業務フロー図(PNG)    |
|   3    | `design-30-screen`           | 画面設計書（項目一覧・アクション・バリデーション・画面遷移）     | 要件定義書・機能一覧・デザイン画像(PNG)    |
|   4    | `design-40-interface`        | インタフェース設計書（画面⇄APIのIF定義・認証認可・エラー設計）   | 要件定義書・機能一覧・画面設計書           |
|   5    | `design-50-api-catalog`      | API一覧（API-ID・method・path・req/res・機能⇄API対応表）         | 機能一覧・インタフェース設計書             |
|   6    | `design-60-process`          | 処理設計書（処理フロー・状態遷移・トランザクション）             | 機能一覧・画面設計書・インタフェース設計書 |
|   7    | `design-70-er-diargram`      | ER図（エンティティ一覧・概念／論理ER図。Mermaid）                | 要件定義書・機能一覧・処理設計書           |
|   8    | `design-80-database`         | DB設計書（テーブル定義・PK/SK・GSI・監査カラム）                 | ER図                                       |

#### 出力

`docs/external-design/product/` 配下の外部設計書一式（アーキテクチャ／シーケンス図／画面設計書／インタフェース設計書／API一覧／処理設計書／ER図／DB設計書）。  

#### 確認ポイント

- 各設計書が前工程の設計書・要件定義と整合しているか
- API一覧と画面・インタフェース設計の対応が取れているか

---

### 4-3. フロントエンド実装

#### 目的

画面設計書・インタフェース設計書に基づき、画面をAIに生成させる。  
フロントエンドは画面単位に実装する。

#### 入力

- 対象画面のデザイン（PNG）
- 画面設計書・インタフェース設計書（`docs/external-design/product/`）
- プロンプト `develop-frontend.prompt.md`

#### 実施手順

1. 対象画面のデザイン（PNG）をCopilot Chatに添付する
2. `develop-frontend.prompt.md` を実行する
3. AIが各ルールファイルの制約や順序に沿って実装を出力する
4. 生成コードと設計書（画面設計・インタフェース設計）の整合性を確認する

補足：フロントエンドは、デザイン（PNG）どおりの見た目・挙動に近づけるため、画像を差し替えながら生成を数回繰り返し、品質を調整する。画面が複数ある場合は、対象画面のデザインを差し替えてプロンプトを繰り返し実行する（例: LGN-001 → TOP-001 → DIV-001/002 → MYP-001）。

#### 出力
`frontend/src/`配下の実装コード、及び単体テストコード

#### 確認ポイント

- 画面設計・インタフェース設計との整合性
- 規約、ルールに沿った実装になっているか
- デザイン（PNG）どおりのレイアウト・挙動になっているか

---

### 4-4. バックエンド実装

#### 目的  

設計書と実装ルールに基づき、バックエンド処理をAIに一括生成させる。

#### 入力

- 設計書一式（機能一覧、画面設計、インタフェース設計、API一覧、処理設計、DB設計、ER図）
- 実装ルール（`backend-rule.md`、`serverless-rule.md`）
- プロンプト `develop-backend.prompt.md`

#### 実施手順

1. `develop-backend.prompt.md` を実行する
2. AIが設計書・ルールを読み込み、バックエンド処理のコードを一括実装を出力する
3. 生成コードを確認する

#### 出力

`backend/`配下の実装コード、及び単体テストコード

#### 確認ポイント

一括生成のため、生成後は特に次の観点を人が確認する。

- API仕様（インタフェース設計・API一覧）との整合性が取れているか
- エラーハンドリングが規約どおりか
- ログ/ファイルの命名などが、ルールファイルに記載された内容に準拠しているか

---

### 4-5. コードレビュー

#### 目的

実装が設計書・規約に沿っているか、セキュリティ上の問題がないかをAIに点検させる。AIレビューはあくまでレビュー支援であり、最終的な採否は開発者が判断する。

#### 入力

- レビュー対象ブランチの変更差分（全コードではなく、当該ブランチで変更した差分をレビューする）
- プロンプト `review-frontend.prompt.md` / `review-backend.prompt.md`

#### 実施手順

1. レビュー対象のブランチで対象のプロンプトを実行する
   - フロントエンド: `review-frontend.prompt.md`
   - バックエンド: `review-backend.prompt.md`
2. AIが観点ベースで変更差分をレビューし、指摘を優先度順に整理する
3. 指摘内容を確認し、対応要否を開発者が判断する（指摘をそのまま採用せず、内容を確認したうえで判断する）  
  
主な観点は次のとおり。

- フロントエンド: 設計書との整合性 / `frontend-rule.md` 遵守 / 型安全性/ テスト不足
- バックエンド: API仕様・設計書との整合性 / `backend-rule.md` 遵守 / セキュリティ（OWASP Top 10）/ 個人情報のログ出力禁止 / 

#### 出力

優先度・種別付きのレビュー指摘一覧。

#### 確認ポイント

指摘は優先度（high / medium / low）と種別で示される。種別ごとに次のとおり対応する。

| 種別          | 対応                                         |
| :------------ | :------------------------------------------- |
| LGTM          | 修正不要                                     |
| Q（Question） | 実装に対する質問。実装意図を実装者に確認する |
| 上記以外      | 修正後、再レビューを実施する                 |

---

### 4-6. 単体テスト

#### 目的

実装の品質をコードレベルで担保する（目標カバレッジは80%以上）。プロンプトを実行すると、テストコードの生成・実行・カバレッジレポート出力までが行われるため、その結果を人が確認して次工程へ進むか判断する。

#### 入力

- 実装済みのフロント・バックのコード
- プロンプト `test-unit-frontend-run.prompt.md` / `test-unit-backend-run.prompt.md`

#### 実施手順

1. プロンプトを実行する  
  `test-unit-frontend-run.prompt.md` / `test-unit-backend-run.prompt.md`
2. プロンプト実行中、AIから次のようなコマンド実行の許可を求められる場合がある。内容を確認したうえで許可する

```bash
 python -m pytest tests/unit/ -v --tb=short 2>&1
```

3. 出力されたテストコード、及びガバレッジレポートを確認する

#### 出力

テストコード（`frontend/tests/` ／ `backend/tests/unit/`）とカバレッジレポート。

#### 確認ポイント

- テストコードを生成して終わりにせず、実際にテストを実行し、期待どおりの結果になっているかを確認する
- カバレッジの数値を満たすだけでなく、重要な業務ロジックが適切にテストできているかを確認する
- カバレッジが100%に満たない場合は、「テストコードが不足しているのか」「そもそもテストできない箇所なのか」を見極め、修正するか次工程へ進むかを切り分ける

---

### 4-7. システムテスト

#### 目的

デプロイ済みの環境に対して、業務シナリオが正しく動作するかをE2E（Playwright）で検証する。ST-01→ST-05の順に実施し、各プロンプトの出力が次タスクの入力となる。  
**補足(1)**：本手順書では、Playwright（テスト自動化ツール）を利用している
**補足(2)**：システムテストはSTG環境で実施する想定としている。本書の場合、AWS環境が構築済みかつデプロイが完了していることを前提としている。  

#### 入力

- 外部設計書一式
- 前工程の出力（観点表 → テストコード → テストデータ → 項目書）

#### 実施手順

各工程の入力・出力は次のとおり。

| 工程                 | プロンプト                 | 入力                         | 出力                                        |
| :------------------- | :------------------------- | :--------------------------- | :------------------------------------------ |
| ST-01 観点表         | `test-system-10-viewpoint` | 設計書                       | テスト観点表（`docs/tests/system-test/`）   |
| ST-02 テストコード   | `test-system-20-testcode`  | 観点表・設計書               | Playwrightテストコード（`frontend/tests/`） |
| ST-03 テストデータ   | `test-system-30-testdata`  | テストコード                 | DynamoDB初期投入データ（JSON/CSV）          |
| ST-04 試験項目書     | `test-system-40-items`     | テストコード                 | システムテスト項目書                        |
| ST-05 実施・結果反映 | `test-system-50-run`       | テストコード・投入済みデータ | テスト結果を反映した項目書                  |

手順の補足

- ST-02のテストコードは、Playwrightのページ操作をクラスにまとめる設計（Page Object Model）で生成される。
- ST-03で生成したテストデータは、ST-05のテスト実施前に開発者がDynamoDBへ投入しておく。

#### 出力

テスト結果（OK/NG）が反映されたシステムテスト項目書。

#### 確認ポイント

- NGの場合は原因を確認し、修正後に再実施する
- 主要な業務フロー（ログイン → 参照 → 更新）が観点として網羅されているか

---

## 5. 補足資料

### A. プロンプト一覧

| フェーズ       | 用途                   | ファイル（`.github/prompts/`）         |
| :------------- | :--------------------- | :------------------------------------- |
| 外部設計       | アーキテクチャ設計     | `design-10-architecture.prompt.md`     |
| 外部設計       | シーケンス図           | `design-20-sequence-diagram.prompt.md` |
| 外部設計       | 画面設計書             | `design-30-screen.prompt.md`           |
| 外部設計       | インタフェース設計書   | `design-40-interface.prompt.md`        |
| 外部設計       | API一覧                | `design-50-api-catalog.prompt.md`      |
| 外部設計       | 処理設計書             | `design-60-process.prompt.md`          |
| 外部設計       | DB設計書               | `design-70-database.prompt.md`         |
| 外部設計       | ER図                   | `design-80-er-diargram.prompt.md`      |
| 実装           | フロントエンド実装     | `develop-frontend.prompt.md`           |
| 実装           | バックエンド一括実装   | `develop-backend.prompt.md`            |
| レビュー       | フロントエンドレビュー | `review-frontend.prompt.md`            |
| レビュー       | バックエンドレビュー   | `review-backend.prompt.md`             |
| 単体テスト     | フロントUT生成         | `test-unit-frontend.prompt.md`         |
| 単体テスト     | フロントUT実施         | `test-unit-frontend-run.prompt.md`     |
| 単体テスト     | バックUT生成           | `test-unit-backend.prompt.md`          |
| 単体テスト     | バックUT実施           | `test-unit-backend-run.prompt.md`      |
| システムテスト | ST-01 観点表           | `test-system-10-viewpoint.prompt.md`   |
| システムテスト | ST-02 テストコード     | `test-system-20-testcode.prompt.md`    |
| システムテスト | ST-03 テストデータ     | `test-system-30-testdata.prompt.md`    |
| システムテスト | ST-04 試験項目書       | `test-system-40-items.prompt.md`       |
| システムテスト | ST-05 テスト実施       | `test-system-50-run.prompt.md`         |

### B. 設計フォーマット一覧（`docs/external-design/format/`）

| 設計書種別           | フォーマットファイル     |
| :------------------- | :----------------------- |
| アーキテクチャ       | `fm-architecture.md`     |
| 画面設計書           | `fm-screen-design.md`    |
| シーケンス図         | `fm-sequence-diagram.md` |
| 処理設計書           | `fm-process-design.md`   |
| ER図                 | `fm-er-diargram.md`      |
| DB設計書             | `fm-database-design.md`  |
| インタフェース設計書 | `fm-interface-design.md` |
| API一覧              | `fm-api-catalog.md`      |

### C. ルール・規約ファイル一覧（`docs/rules/`）

| カテゴリ               | ファイルパス                     |
| :--------------------- | :------------------------------- |
| 命名規則               | `common/naming-rule.md`          |
| エラー設計規則         | `common/error-rule.md`           |
| 用語集                 | `common/glossary.md`             |
| 実装規約               | `common/implementation-rule.md`  |
| API設計規則            | `design/api-design-rule.md`      |
| DB設計規則             | `design/db-design-rule.md`       |
| 画面設計規則           | `design/screen-design-rule.md`   |
| 処理設計規則           | `design/process-design-rule.md`  |
| フロントエンド実装規約 | `development/frontend-rule.md`   |
| バックエンド実装規約   | `development/backend-rule.md`    |
| サーバレス実装規約     | `development/serverless-rule.md` |
| テスト規約             | `development/test-rule.md`       |

### D. ローカル開発環境セットアップ（コマンドまとめ）

**補足** `moto` は、AWS上のDynamoDBを利用しなくても、ローカルでDynamoDB相当の動作確認ができるテスト用サーバー。`moto_server` で起動する（ターミナル1は起動したままにしておく）。

```bash
# ターミナル1：バックエンド仮想環境のセットアップ
cd backend
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
pip install -r requirements-dev.txt

# ターミナル1：moto サーバー起動（DynamoDB エミュレーター、ポート 8000）
moto_server dynamodb -H localhost -p 8000

# ターミナル2：テーブル作成 → API サーバー起動
cd backend
.venv\Scripts\activate
python infra/scripts/create_local_tables.py
uvicorn app.main:app --reload

# ターミナル3：フロントエンド
cd frontend
npm install
npm run dev
```

### E. テストツール及び実行コマンドまとめ

本手順書では、下記のツールを使用している。

| 対象           | ツール                           |
| :------------- | :------------------------------- |
| フロントエンド | Vitest + React Testing Library   |
| バックエンド   | pytest + moto（DynamoDB モック） |

実行コマンド:

```bash
# フロントエンド 単体テスト（カバレッジ）
cd frontend; npm run test -- --coverage

# バックエンド 単体テスト（カバレッジ）
cd backend; pytest --cov=app --cov-report=term-missing

# E2E（システムテスト）
cd frontend; npm run test:e2e -- --project=chromium
```

### F. 重要注意事項

| 区分         | 注意事項                                                             |
| :----------- | :------------------------------------------------------------------- |
| セキュリティ | `.env` ファイルを Git にコミットしない（`.gitignore` を確認）        |
| 個人情報     | 組合員情報（氏名・住所・口座番号等）をログに出力しない（マスキング） |
| Lambda       | ステートレス設計を守る（グローバル状態への依存禁止）                 |
| デプロイ     | `main` へのマージは必ずレビュー・テスト通過後に行う                  |
| AWS          | IAM ポリシーは最小権限の原則に従う                                   |

### G. AWS環境構築（AWS環境構築担当者向け・簡易）

> この付録はAWS環境を構築する担当者向けである。ローカル開発のみを行う場合は不要。詳細な手順は別資料（AWS環境構築編）を参照する。

AWSマネジメントコンソールで、`database-design.md` の設計に従い次のリソースを構築する。

| リソース    | 主な設定                                                                                                                        |
| :---------- | :------------------------------------------------------------------------------------------------------------------------------ |
| DynamoDB    | `members` / `distributions` / `auth_sessions` / `notice_reads` テーブル。キャパシティはオンデマンド。必要に応じてGSI・TTLを設定 |
| Lambda      | ランタイム Python 3.12、ハンドラ `app.main.handler`、タイムアウト 900秒。環境変数にテーブル名・リージョン等を設定               |
| API Gateway | HTTP API。Lambda統合、ルート `ANY /{proxy+}`。CORSで許可オリジン・ヘッダー・メソッドを設定                                      |
| S3          | フロント配信用バケット。パブリックアクセスは全てブロック（CloudFront経由で配信）                                                |
| CloudFront  | オリジンにS3を指定、OACでアクセス制御。HTTPS強制、SPA用に403/404を`/index.html`(200)へ                                          |
| IAM         | Lambda実行ロール（DynamoDB最小権限）、GitHub Actions用ロール（OIDC連携）                                                        |

### H. デプロイ環境構築 CI/CD（CI/CD構築担当者向け・簡易）

この付録はCI/CDを構築する担当者向けである。ローカル開発のみを行う場合は不要。詳細な手順は別資料（CI/CD構築編）を参照する。  
`main` ブランチへのマージをトリガーに、テスト・ビルド・デプロイを自動化する（`.github/workflows/deploy-frontend.yml` / `deploy-backend.yml`）。

#### GitHub Secrets（Settings → Secrets and variables → Actions）

| Secret名                     | 値                                   |
| :--------------------------- | :----------------------------------- |
| `AWS_BACKEND_ROLE_ARN`       | バックエンド用 IAM ロールの ARN      |
| `AWS_FRONTEND_ROLE_ARN`      | フロントエンド用 IAM ロールの ARN    |
| `S3_BUCKET_NAME`             | フロントエンド用 S3 バケット名       |
| `CLOUDFRONT_DISTRIBUTION_ID` | CloudFront ディストリビューション ID |

#### デプロイの流れ（概要）

| 対象           | 流れ                                                                           |
| :------------- | :----------------------------------------------------------------------------- |
| フロントエンド | lint → test → build → S3へアップロード → CloudFrontキャッシュ無効化            |
| バックエンド   | pytest → デプロイパッケージ作成 → `aws lambda update-function-code` でデプロイ |

---
